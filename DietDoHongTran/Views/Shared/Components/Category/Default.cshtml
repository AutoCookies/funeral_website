@model List<DietDoHongTran.ViewModels.CategoryViewModel>

<div class="p-2" aria-labelledby="categoryDropdown">
    @foreach (var category in Model)
    {
        <a class="dropdown-item" href="@category.Href">
            @category.Name
        </a>
    }
</div>
